{
  "name": "bezier-paths",
  "version": "1.0.1",
  "description": "A library to easily generate and traverse bezier paths. And different parts of the path can have different speeds!",
  "main": "dist/index.js",
  "scripts": {
    "build": "parcel build index.js --global BezierPaths -d dist && parcel build index.js --global BezierPaths -d docs -o bezier-paths.js"
  },
  "keywords": [
    "bezier",
    "library",
    "js",
    "path",
    "traverse"
  ],
  "author": "Maurycy Zarzycki Evidently Cube",
  "license": "MIT",
  "devDependencies": {
    "parcel": "^1.12.3"
  }
}
